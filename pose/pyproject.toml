[project]
name = "pose"
version = "0.1.0"
description = "Advanced motion retargeter"
authors = [
    {name = "Xue Bin Peng", email = "xbpeng@sfu.ca"}
]
license = {text = "BSD-3-Clause"}
requires-python = ">=3.6"

dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pose"]
